// Wait for the entire DOM content to be loaded and parsed before executing the code
document.addEventListener('DOMContentLoaded', () => {
  // Logs a message to the console indicating that the portfolio page has successfully loaded
  console.log('Portfolio loaded!'); 

  // Additional JavaScript functionality can be added here
  // For example, setting up event listeners or initializing dynamic features
});
